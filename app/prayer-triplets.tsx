import { PlaceholderScreen } from '../components/PlaceholderScreen';

export default function PrayerTriplets() {
  return (
    <PlaceholderScreen
      title="Prayer Triplets"
      note="Draft placeholder. Kept distinct from Community Groups per the Small Group Strategy (v2) document — same/similar sex, 3-4 people, open and accountable. Reached from both Make → Pray Always and Disciples → Family."
    />
  );
}
