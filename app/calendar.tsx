import { PlaceholderScreen } from '../components/PlaceholderScreen';

export default function Calendar() {
  return (
    <PlaceholderScreen
      title="Calendar"
      note="Draft placeholder. Single shared calendar — this same screen is what's linked to from Many, Disciples → Magnify, and the Utility menu, each time filtered by tag rather than duplicated."
    />
  );
}
