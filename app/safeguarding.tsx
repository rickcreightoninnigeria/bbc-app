import { PlaceholderScreen } from '../components/PlaceholderScreen';

export default function Safeguarding() {
  return (
    <PlaceholderScreen
      title="Safeguarding & Policies"
      note="Draft placeholder. Compliance content — deliberately not a nav-visible 'value', linked in from here and from Deep → Build the Next Generation."
    />
  );
}
